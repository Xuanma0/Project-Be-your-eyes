# Risk + POV Combo Fixture

This fixture combines:
- `run_package_with_risk_gt_min` (risk ground truth and events)
- `pov_ir_v1_min` (`pov/pov_ir_v1.json`)

Use it for POV budget ablation smoke checks where both context and risk metrics should be available.
